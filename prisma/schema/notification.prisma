model Notification{
    id          String          @id @default(uuid())
    title       String
    content     String
    users       User_Notification[]
    createdAt   DateTime        @default(now())

  @@map("notifications")
}
