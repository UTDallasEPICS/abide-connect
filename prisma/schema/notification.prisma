model Notification{
    id          String          @id @default(uuid())
    title       String
    content     String
    user        User_Notification[]
    createdAt   DateTime        @default(now())

    @@map("notifications")
}