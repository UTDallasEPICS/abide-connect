model Donation {
  id              String   @id @default(uuid())
  name            String      
  startDate       DateTime
  endDate         DateTime
  link            String
  imageUrl        String

  createdAt DateTime @default(now())
  updatedAt DateTime @default(now()) @updatedAt
}